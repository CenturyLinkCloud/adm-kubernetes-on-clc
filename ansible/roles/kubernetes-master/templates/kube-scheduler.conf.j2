description "kube-scheduler service"
author "mark ramach"

script

	KUBE_SCHEDULER_OPTS="--address=0.0.0.0 --master=http://{{ ansible_default_ipv4.address }}:8080"

	exec kube-scheduler $KUBE_SCHEDULER_OPTS > /var/log/kube-scheduler.log

end script